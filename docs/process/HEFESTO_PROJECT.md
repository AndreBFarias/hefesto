# HEFESTO — Prompt de Projeto Completo

> Porte espiritual do DualSenseX (Paliverse) para Linux em Python.
> Daemon + TUI + CLI + IPC. Sem Wine, sem .NET, sem UI proprietária.

```
STATUS: SCAFFOLD | VERSION: 0.1.0 | LANG: PT-BR | TARGET: Linux/Python 3.10+
DECISOES CANONICAS: docs/process/HEFESTO_DECISIONS_V2.md + V3.md
```

---

## FONTES CANÔNICAS (ler antes deste spec)

Duas rodadas de auditoria pós-escrita deste documento produziram deltas que **sobrescrevem** partes deste spec. Em qualquer conflito, obedecer nesta ordem:

1. `~/.config/zsh/AI.md` v4.0 (regras universais de projeto).
2. `~/.claude/CLAUDE.md` (extensões: meta-regras 9.6–9.8, validação visual 13–14).
3. `docs/process/HEFESTO_DECISIONS_V2.md` (patches 1–11 consolidados).
4. `docs/process/HEFESTO_DECISIONS_V3.md` (deltas V3-1 a V3-8 + correções sobre patches V2).
5. Este arquivo (contexto e waves 0–8).

Itens deste spec afetados por patches V2/V3:

- Sprint 0.1 (§ WAVE 0) → V2 Patch 1 + V3 § 4 (total 22 itens).
- Interface `IController` (§ WAVE 1 / Sprint 1.1) → V2 Patch 2.
- Schema de perfil e `match` (§ PERFIS) → V2 Patch 3.
- Rate limit UDP (§ WAVE 4 / Sprint 4.3) → V3-1 (substitui V2 Patch 4).
- udev rules (§ REGRA ZERO) → V2 Patch 5.
- Units systemd (§ WAVE 4 / Sprint 4.1) → V2 Patch 8 + V2-12.
- Hotkey combo (§ WAVE 8 / Sprint 8.1) → V2 Patch 9 + V3-2.
- ADR-008, ADR-009 → V2 Patches 7, 10.
- TUI offline (§ WAVE 5 / Sprint 5.1) → V2 Patch 11 + V3-3.

---

# REGRA -1: ANONIMATO ABSOLUTO

## ISTO E INVIOLAVEL. LEIA ANTES DE QUALQUER ACAO.

### PALAVRAS PROIBIDAS EM QUALQUER ARQUIVO DO REPO:

```
BLOQUEADO:
├── "Claude" / "claude" / "CLAUDE"
├── "GPT" / "ChatGPT" / "gpt-4" / "Opus" / "Sonnet"
├── "Anthropic" / "OpenAI"
├── "by Claude" / "by AI" / "AI-generated" / "Generated by"
├── "Feito por" / "Criado por" / "Autor: <nome>"
├── Nomes de pessoas reais (Andre, Vitoria, etc.)
├── Emails pessoais
├── @usernames
└── Assinaturas em comentarios ou docstrings
```

### VALIDACAO PRE-COMMIT (DEVE RETORNAR VAZIO):

```bash
grep -rniE "claude|anthropic|openai|gpt-[0-9]|chatgpt" src/ tests/ docs/ \
  --include="*.py" --include="*.md" --include="*.toml" \
  | grep -viE "api_key|provider|model|config|client|engine"
```

### SE VOCE (IA EXECUTORA) ESCREVER "CLAUDE":

```
1. PARE IMEDIATAMENTE
2. DELETE a linha
3. VERIFIQUE o arquivo inteiro com grep
4. SO ENTAO continue
```

**O codigo pertence a quem o executa. Nao a quem o escreve.**

---

# REGRA ZERO: GITHUB WORKFLOW

## TODA tarefa usa este fluxo. Sem excecoes.

### 1. PEGAR ISSUE

```bash
gh issue list --label "status:ready"
gh issue view NUMERO
gh issue edit NUMERO --add-label "status:in-progress" --remove-label "status:ready"
```

### 2. CRIAR BRANCH

```bash
gh issue develop NUMERO --checkout
```

### 3. COMMITAR

```bash
git commit -m "feat: descricao tecnica impessoal"
```

### 4. VALIDAR LOCALMENTE

```bash
./scripts/check_anonymity.sh   # deve retornar vazio
ruff check src/
mypy src/hefesto
pytest tests/ -v
```

### 5. PR QUE FECHA ISSUE

```bash
gh pr create --title "feat: X" --body "Closes #NUMERO"
gh pr merge --squash --delete-branch
```

---

# VISAO DO PRODUTO

## O que o DualSenseX faz (Windows):

1. Fala HID com o controle DualSense (VID 054c, PID 0ce6).
2. Aplica efeitos de gatilho adaptativo em background (19 modos).
3. Expoe protocolo UDP local (porta 6969 no Win) pra jogos/mods mudarem estado em tempo real.
4. Emula Xbox360/DS4 via ViGEmBus (pra jogos que nao reconhecem DualSense).
5. UI pra testar/configurar efeitos, LEDs, rumble.

## O que Hefesto vai fazer (Linux):

| Feature DSX           | Hefesto Linux                                      | Sprint |
|-----------------------|----------------------------------------------------|--------|
| HID direto            | `pydualsense` como core, hidapi via libhidapi-dev  | W1     |
| 19 trigger modes      | Todos mapeados via `TriggerState`                  | W2     |
| UDP localhost         | Socket UDP na mesma porta 6969 (compat com mods)   | W4     |
| Emulacao Xbox360      | `python-uinput` → gera /dev/input/js* virtual      | W6     |
| UI Windows            | TUI Textual + CLI + tray opcional                  | W5     |
| Perfis por jogo       | JSON + auto-switch por janela X11 ativa            | W3, W6 |
| Auto-start            | `systemd --user` unit                              | W4     |
| LEDs/Rumble           | Via pydualsense                                    | W2     |

**Nao faremos:**

- Suporte Windows (fora de escopo).
- macOS (HID da Sony nao documenta bem).
- Wayland nativo na v0.x (X11-first; Wayland via ADR-007).
- Bluetooth Audio (protocolo fechado da Sony, DSX-Steam pago faz isso).

---

# STACK TECNICA

```python
Python >= 3.10                  # match-case para trigger modes
pydualsense >= 0.7.5            # HID backend (MIT license)
hidapi (sistema) + libhidapi-dev
textual >= 0.47                 # TUI
typer >= 0.9                    # CLI
pydantic >= 2.0                 # validacao de perfis JSON
python-xlib >= 0.33             # deteccao de janela ativa X11
evdev >= 1.6                    # leitura de teclas globais pra hotkey
python-uinput >= 0.11           # emulacao gamepad virtual (W6)
structlog >= 23.0               # logging estruturado
pytest + pytest-asyncio
ruff + mypy
```

**Justificativas:**

- `pydualsense` > implementacao do zero: 115 commits, protocolo HID completo, LICENSE MIT. Reimplementar seria 2 sprints perdidas.
- `textual` > curses: mesma stack do Luna, tema Dracula ja resolvido.
- `typer` > argparse: decorators sao mais limpos, bom pra CLI com subcomandos.
- `python-xlib` > `xdotool` via subprocess: menos overhead, sem shell-out.
- `evdev` > pynput: funciona sem display server, essencial pra hotkey global.
- `uinput` > `python-vgamepad`: vgamepad e Windows/ViGEmBus-only.

---

# ARQUITETURA

```
hefesto/
├── pyproject.toml
├── README.md
├── AGENTS.md                          # protocolo de IA (era CLAUDE.md no draft; V2-2)
├── CHECKLIST_MANUAL.md                # smoke tests com device fisico (V2-14)
├── LICENSE                            # MIT
├── NOTICE                             # atribuicao da udev rule (V2 5.3)
├── .github/
│   ├── ISSUE_TEMPLATE/
│   │   ├── bug.md
│   │   ├── feature.md
│   │   └── sprint-task.md
│   ├── workflows/
│   │   ├── ci.yml                     # lint + test + anonimato
│   │   └── release.yml                # build wheel + PyPI publish
│   └── dependabot.yml
├── scripts/
│   ├── check_anonymity.sh             # grep pre-commit
│   ├── check_test_data.sh             # sem nomes reais em testes
│   ├── install_udev.sh                # instala 70-ps5-controller.rules
│   └── dev_bootstrap.sh               # cria venv, instala deps, roda testes
├── docs/
│   ├── adr/                           # Architecture Decision Records
│   │   ├── 001-pydualsense-backend.md
│   │   ├── 002-textual-tui.md
│   │   ├── 003-udp-port-6969-compat.md
│   │   ├── 004-systemd-user-service.md
│   │   ├── 005-profile-schema-v1.md
│   │   ├── 006-xlib-window-detection.md
│   │   └── 007-wayland-deferral.md
│   ├── protocol/
│   │   ├── udp-schema.md              # compat DSX
│   │   └── ipc-unix-socket.md
│   └── usage/
│       ├── quickstart.md
│       ├── creating-profiles.md
│       └── integrating-mods.md
├── assets/
│   ├── 70-ps5-controller.rules        # udev rules (do pydualsense, MIT)
│   ├── hefesto.service                # systemd --user unit
│   ├── hefesto.desktop                # entry pra tray
│   └── profiles_default/
│       ├── driving.json
│       ├── shooter.json
│       ├── bow.json
│       └── fallback.json
├── src/
│   └── hefesto/
│       ├── __init__.py
│       ├── __main__.py                # python -m hefesto
│       ├── config.py                  # paths, XDG dirs, env vars
│       ├── core/
│       │   ├── __init__.py
│       │   ├── controller.py          # IController interface
│       │   ├── backend_pydualsense.py # implementacao
│       │   ├── trigger_effects.py     # 19 modos + factories
│       │   ├── led_control.py
│       │   ├── rumble.py
│       │   └── events.py              # pubsub interno
│       ├── daemon/
│       │   ├── __init__.py
│       │   ├── main.py                # entry do daemon
│       │   ├── lifecycle.py           # start/stop/reload
│       │   ├── ipc_server.py          # Unix socket
│       │   ├── udp_server.py          # porta 6969 compat DSX
│       │   └── state_store.py         # estado atual do controle
│       ├── profiles/
│       │   ├── __init__.py
│       │   ├── schema.py              # pydantic models
│       │   ├── loader.py              # read/write JSON
│       │   ├── manager.py             # switch ativo
│       │   └── autoswitch.py          # detecta janela → troca perfil
│       ├── cli/
│       │   ├── __init__.py
│       │   ├── app.py                 # typer entry
│       │   ├── cmd_status.py
│       │   ├── cmd_profile.py
│       │   ├── cmd_test.py
│       │   └── cmd_daemon.py
│       ├── tui/
│       │   ├── __init__.py
│       │   ├── app.py                 # Textual App
│       │   ├── screens/
│       │   │   ├── main.py
│       │   │   ├── profiles.py
│       │   │   ├── test_trigger.py
│       │   │   └── settings.py
│       │   ├── widgets/
│       │   │   ├── trigger_preview.py
│       │   │   ├── battery_meter.py
│       │   │   └── status_bar.py
│       │   └── theme.py               # Dracula
│       ├── integrations/
│       │   ├── __init__.py
│       │   ├── xlib_window.py         # janela ativa X11
│       │   ├── uinput_gamepad.py      # gamepad virtual (W6)
│       │   └── hotkey_evdev.py        # hotkeys globais
│       └── utils/
│           ├── __init__.py
│           ├── logging_config.py
│           ├── xdg_paths.py
│           └── file_lock.py           # lock pra config file
├── tests/
│   ├── unit/
│   │   ├── test_trigger_effects.py
│   │   ├── test_profile_schema.py
│   │   ├── test_udp_server.py
│   │   └── test_autoswitch.py
│   ├── integration/
│   │   ├── test_daemon_lifecycle.py
│   │   └── test_ipc_roundtrip.py
│   ├── fixtures/
│   │   └── profiles/
│   └── conftest.py
└── examples/
    ├── change_trigger_cli.sh
    ├── custom_profile.json
    └── mod_integration_udp.py
```

---

# PROTOCOLO UDP (compat DSX)

## Objetivo

Mods existentes pra Cyberpunk/Forza/Assetto Corsa escrevem em UDP localhost quando rodando via Proton. Hefesto escuta a MESMA porta e traduz pro protocolo HID.

## Schema (v1)

```json
{
  "version": 1,
  "instructions": [
    {
      "type": "TriggerUpdate",
      "parameters": ["left", "Rigid", 10, 255, 0, 0, 0, 0, 0]
    },
    {
      "type": "RGBUpdate",
      "parameters": [0, 255, 100, 50]
    },
    {
      "type": "TriggerThreshold",
      "parameters": ["right", 128]
    }
  ]
}
```

## Tipos de instrucao suportados (v1)

- `TriggerUpdate(side, mode, *params)` — aplica trigger effect
- `RGBUpdate(controller_idx, r, g, b)` — cor da lightbar
- `PlayerLED(idx, bitmask)` — 5 LEDs inferiores
- `MicLED(state)` — LED do microfone
- `TriggerThreshold(side, value)` — threshold de ativacao
- `ResetToUserSettings()` — volta pro perfil do Hefesto

## Porta

`127.0.0.1:6969` (mesma do DSX Windows). Configuravel via `~/.config/hefesto/daemon.toml`.

---

# PROTOCOLO IPC (Unix socket)

Usado pela TUI e CLI pra falar com o daemon.

**Path:** `$XDG_RUNTIME_DIR/hefesto.sock`

**Formato:** JSON-RPC 2.0 sobre linha-a-linha.

```json
{"jsonrpc": "2.0", "method": "profile.switch", "params": {"name": "shooter"}, "id": 1}
{"jsonrpc": "2.0", "method": "trigger.set", "params": {"side": "left", "mode": "Machine", "params": [0,9,7,7,10,0]}, "id": 2}
{"jsonrpc": "2.0", "method": "daemon.status", "id": 3}
{"jsonrpc": "2.0", "method": "controller.list", "id": 4}
```

**Resposta:**

```json
{"jsonrpc": "2.0", "result": {"status": "ok", "active_profile": "shooter"}, "id": 1}
```

---

# PERFIS (schema v1)

```json
{
  "name": "cyberpunk_driving",
  "version": 1,
  "match": {
    "window_class": ["steam_app_1091500"],
    "window_title_regex": ".*Cyberpunk.*",
    "process_name": ["Cyberpunk2077.exe"]
  },
  "priority": 10,
  "triggers": {
    "left": {
      "mode": "Medium",
      "params": []
    },
    "right": {
      "mode": "Galloping",
      "params": [0, 9, 7, 7, 10]
    }
  },
  "leds": {
    "lightbar": [255, 100, 0],
    "player_leds": [false, true, true, true, false]
  },
  "rumble": {
    "passthrough": true
  }
}
```

**Match logic:** aplica perfil de maior `priority` cujo `match` combine com janela ativa. `fallback.json` tem `priority: 0` e matcher `*`.

---

# PLANO DE EXECUCAO — 8 WAVES, 26 SPRINTS

## WAVE 0 — Bootstrap (1 sprint)

### Sprint 0.1: Repo + Tooling

**Versão canônica desta sprint: `HEFESTO_DECISIONS_V2.md` Patch 1 + `HEFESTO_DECISIONS_V3.md` § 4.**

Resumo (22 itens). Leia o arquivo canônico pro detalhe:

1-9. Repo, estrutura, `pyproject.toml` com deps + extras, `.gitattributes`, `NOTICE`, `LICENSE`, `check_anonymity.sh`, `check_test_data.sh`, `dev_bootstrap.sh`.
10-13. `install_udev.sh`, `setup_labels.sh`, `setup_issues.sh`, CI workflow.
14-19. 9 ADR stubs, `trigger-modes.md`, `HEFESTO_DECISIONS_V2.md` em `docs/process/`, `CHECKLIST_MANUAL.md`, `AGENTS.md`, `README.md`.
20. `tests/unit/test_check_anonymity.py` (V3-7).
21. Gate de 5MB pros HID captures no CI (V3-5).
22. `scripts/record_hid_capture.py` + `captures/script_default.yaml` (V3-8).

**Definition of Done canônica:** ver `HEFESTO_DECISIONS_V2.md` Patch 1, atualizada com itens V3.

---

## WAVE 1 — HID Core (3 sprints)

### Sprint 1.1: Backend abstraction + pydualsense
- [ ] Interface `IController` (ABC) com metodos: `connect`, `disconnect`, `read_state`, `set_trigger`, `set_led`, `set_rumble`, `get_battery`, `is_connected`.
- [ ] `backend_pydualsense.py` implementa `IController`.
- [ ] Teste unitario com mock do `pydualsense`.

### Sprint 1.2: Event bus + state store
- [ ] `events.py`: pubsub com asyncio.Queue pra: `button_down`, `button_up`, `battery_change`, `connected`, `disconnected`.
- [ ] `daemon/state_store.py`: thread-safe dict com estado atual (bateria, trigger modes ativos, perfil).

### Sprint 1.3: Daemon loop basico
- [ ] `daemon/main.py`: asyncio event loop.
- [ ] Conecta no controle, poll 60Hz, publica events.
- [ ] `hefesto daemon start` liga, `Ctrl+C` desliga limpo.
- [ ] Logging estruturado com structlog.

**DoD:** `hefesto daemon start` conecta no DualSense fisico, mostra bateria e input do stick.

---

## WAVE 2 — Trigger Effects (2 sprints)

### Sprint 2.1: 19 modos mapeados
- [ ] `trigger_effects.py`: `TriggerMode` enum + `TriggerEffect` dataclass.
- [ ] Factories: `rigid()`, `pulse()`, `galloping(freq, start, end, strength)`, `machine(...)`, etc.
- [ ] Validacao de params (ranges do README do DSX).
- [ ] Tabela de compatibilidade documentada em `docs/protocol/trigger-modes.md`.

### Sprint 2.2: LEDs e rumble
- [ ] `led_control.py`: lightbar RGB, player LEDs, mic LED.
- [ ] `rumble.py`: weak + strong motor com throttle anti-spam.
- [ ] Teste com placeholder `hefesto test trigger --mode galloping --side right`.

**DoD:** Todos os 19 modos testaveis via CLI, produzem vibracao correta no controle fisico.

---

## WAVE 3 — Sistema de Perfis (2 sprints)

### Sprint 3.1: Schema + loader
- [ ] `profiles/schema.py`: modelos pydantic v2.
- [ ] `profiles/loader.py`: read/write JSON com `file_lock`.
- [ ] XDG paths: `~/.config/hefesto/profiles/*.json`.
- [ ] 4 perfis default em `assets/profiles_default/`.

### Sprint 3.2: Manager + CLI
- [ ] `profiles/manager.py`: listar, ativar, criar, deletar.
- [ ] `hefesto profile list | activate <nome> | show <nome>`.
- [ ] Teste: ativar perfil aplica triggers corretos no controle.

**DoD:** Trocar perfil via CLI muda efeito no controle em < 100ms.

---

## WAVE 4 — Daemon + IPC (3 sprints)

### Sprint 4.1: systemd --user service
- [ ] `assets/hefesto.service` com `Restart=on-failure`, `After=graphical-session.target`.
- [ ] `hefesto daemon install-service` copia pra `~/.config/systemd/user/` e `systemctl --user enable`.
- [ ] Teste: reboot → daemon sobe sozinho.

### Sprint 4.2: IPC Unix socket (JSON-RPC 2.0)
- [ ] `daemon/ipc_server.py`: asyncio Unix socket server.
- [ ] 8 metodos v1: `profile.switch`, `profile.list`, `trigger.set`, `trigger.reset`, `led.set`, `daemon.status`, `controller.list`, `daemon.reload`.
- [ ] Cliente em `cli/` usa mesmo protocolo.

### Sprint 4.3: UDP server compat DSX
- [ ] `daemon/udp_server.py`: escuta `127.0.0.1:6969`.
- [ ] Parser do schema DSX JSON.
- [ ] Rate limit: 1000 pacotes/s por cliente.
- [ ] Teste integracao: roda mod de exemplo e confirma efeito aplicado.

**DoD:** `systemctl --user status hefesto` mostra ativo. `python examples/mod_integration_udp.py` aplica efeito.

---

## WAVE 5 — Interface (4 sprints)

### Sprint 5.1: TUI base (Textual)
- [ ] `tui/app.py`: App com tema Dracula.
- [ ] `Screens`: Main, Profiles, TestTrigger, Settings.
- [ ] StatusBar no rodape: bateria, perfil ativo, connected.

### Sprint 5.2: Preview widgets
- [ ] `trigger_preview.py`: barra visual animada do estado L2/R2 (0-255).
- [ ] `battery_meter.py`: com cor que muda (verde → vermelho).
- [ ] Atualizacao via event bus do daemon (via IPC).

### Sprint 5.3: CLI completo (typer)
- [ ] `hefesto status` — tabela com info do daemon.
- [ ] `hefesto profile {list,show,activate,create,delete,edit}`.
- [ ] `hefesto test trigger --side --mode --params`.
- [ ] `hefesto daemon {start,stop,restart,logs,status}`.
- [ ] `hefesto led --color HEX` + `hefesto battery`.
- [ ] Tab completion via `typer --install-completion`.

### Sprint 5.4: Tray opcional
- [ ] `assets/hefesto.desktop` + icone SVG.
- [ ] Tray GTK3 com AppIndicator3 (opcional, extra dep).
- [ ] Menu: perfis, status, open TUI, quit.

**DoD:** Usuario configura tudo sem abrir editor de texto.

---

## WAVE 6 — Auto-switching + Emulacao (3 sprints)

### Sprint 6.1: Window detection X11
- [ ] `integrations/xlib_window.py`: poll 2Hz da janela ativa.
- [ ] Retorna: `{wm_class, wm_name, pid, exe_name}`.
- [ ] Teste em varios apps (Steam, Firefox, terminais).

### Sprint 6.2: Autoswitch
- [ ] `profiles/autoswitch.py`: escuta mudanca de janela → escolhe perfil.
- [ ] Debounce 500ms pra evitar flicker em alt-tab.
- [ ] Log: `INFO profile changed from=shooter to=driving window=Cyberpunk2077`.

### Sprint 6.3: Gamepad virtual (uinput)
- [ ] `integrations/uinput_gamepad.py`: cria `/dev/input/js*` Xbox360.
- [ ] Modo toggle: `hefesto emulate xbox360 --on`.
- [ ] Esconde HID real via `HidHide`-equivalente (udev trick).

**DoD:** Cyberpunk lancado via Steam troca perfil automaticamente. Jogo que so reconhece Xbox360 ve gamepad virtual.

---

## WAVE 7 — Packaging (2 sprints)

### Sprint 7.1: PyPI
- [ ] `pyproject.toml` pronto pra `python -m build`.
- [ ] Entry points: `hefesto` → `cli.app:main`.
- [ ] Teste: `pipx install hefesto` funciona.
- [ ] Release workflow GH Actions.

### Sprint 7.2: AppImage (opcional)
- [ ] Script com `python-appimage`.
- [ ] Bundle inclui `libhidapi`.
- [ ] Teste em Ubuntu 22.04, Pop!_OS 22.04, Fedora 39.

**DoD:** Download → chmod +x → executa sem deps do sistema (exceto udev rule).

---

## WAVE 8 — Docs + Hotkeys + Polish (2 sprints)

### Sprint 8.1: Hotkeys globais
- [ ] `integrations/hotkey_evdev.py`: le `/dev/input/event*` pra combos (precisa udev rule em `input`).
- [ ] Config: `{"ctrl+alt+1": "profile.activate(shooter)"}`.
- [ ] Teste: hotkey funciona com janela de jogo em foco.

### Sprint 8.2: Docs + release 1.0
- [ ] `docs/usage/quickstart.md` com gif (peek/asciinema).
- [ ] 7 ADRs finalizadas.
- [ ] README com matriz de compatibilidade (distros testadas).
- [ ] Changelog.
- [ ] Tag `v1.0.0`.

---

# PRIMEIRA SPRINT — DETALHAMENTO EXECUTAVEL

## Sprint 0.1 — passo a passo pro Claude Code Opus

### 1. Criar repo

```bash
mkdir hefesto && cd hefesto
git init -b main
gh repo create hefesto --public --source=. --description "Linux adaptive trigger daemon for DualSense controllers"
```

### 2. Estrutura de pastas

```bash
mkdir -p src/hefesto/{core,daemon,profiles,cli,tui/screens,tui/widgets,integrations,utils}
mkdir -p tests/{unit,integration,fixtures/profiles}
mkdir -p docs/{adr,protocol,usage}
mkdir -p assets/profiles_default scripts examples
mkdir -p .github/ISSUE_TEMPLATE .github/workflows
touch src/hefesto/__init__.py
```

### 3. pyproject.toml

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hefesto"
version = "0.1.0"
description = "Linux adaptive trigger daemon for DualSense controllers"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = ["dualsense", "ps5", "adaptive-triggers", "linux", "gamepad"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "pydualsense>=0.7.5",
    "textual>=0.47",
    "typer>=0.9",
    "pydantic>=2.0",
    "python-xlib>=0.33",
    "evdev>=1.6",
    "structlog>=23.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.1",
    "ruff>=0.1",
    "mypy>=1.7",
    "types-python-xlib",
]
emulation = [
    "python-uinput>=0.11",
]
tray = [
    "PyGObject>=3.44",
]

[project.scripts]
hefesto = "hefesto.cli.app:main"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

### 4. scripts/check_anonymity.sh

```bash
#!/usr/bin/env bash
set -euo pipefail

FORBIDDEN='claude|anthropic|openai|chatgpt|gpt-[0-9]|by AI|AI-generated'
ALLOWED_CONTEXT='api_key|provider|model|config|client|engine'

HITS=$(grep -rniE "$FORBIDDEN" src/ tests/ docs/ assets/ scripts/ \
    --include="*.py" --include="*.md" --include="*.toml" --include="*.sh" \
    2>/dev/null | grep -viE "$ALLOWED_CONTEXT" || true)

if [[ -n "$HITS" ]]; then
    echo "ANONIMATO VIOLADO:"
    echo "$HITS"
    exit 1
fi

echo "OK: anonimato preservado."
```

### 5. .github/workflows/ci.yml

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  anonymity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/check_anonymity.sh

  lint-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: |
          sudo apt-get update
          sudo apt-get install -y libhidapi-dev libudev-dev libxi-dev
      - run: pip install -e ".[dev]"
      - run: ruff check src/ tests/
      - run: mypy src/hefesto
      - run: pytest tests/unit -v --cov=hefesto
```

### 6. assets/70-ps5-controller.rules

```
# DualSense controller access without root
# USB
KERNEL=="hidraw*", SUBSYSTEM=="hidraw", ATTRS{idVendor}=="054c", ATTRS{idProduct}=="0ce6", MODE="0666"
# Bluetooth
KERNEL=="hidraw*", SUBSYSTEM=="hidraw", KERNELS=="0005:054C:0CE6.*", MODE="0666"
```

### 7. scripts/install_udev.sh

```bash
#!/usr/bin/env bash
set -euo pipefail

sudo cp assets/70-ps5-controller.rules /etc/udev/rules.d/
sudo udevadm control --reload-rules
sudo udevadm trigger

echo "udev rule instalada. Desconecte e reconecte o controle."
```

### 8. assets/hefesto.service

```ini
[Unit]
Description=Hefesto DualSense adaptive trigger daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/hefesto daemon start --foreground
Restart=on-failure
RestartSec=2
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=graphical-session.target
```

### 9. .github/ISSUE_TEMPLATE/sprint-task.md

```markdown
---
name: Sprint Task
about: Tarefa rastreavel de uma sprint
labels: ["type:feature", "ai-task", "status:ready"]
---

## Contexto
Sprint: **W?.?**
Referencia: `HEFESTO_PROJECT.md` secao X

## Criterios de aceite
- [ ] ...
- [ ] Anonimato: `./scripts/check_anonymity.sh` vazio
- [ ] Testes novos passando
- [ ] Docs atualizadas se aplicavel

## DoD
...
```

### 10. Primeiro commit

```bash
git add .
git commit -m "chore: scaffold inicial do projeto"
git push -u origin main
```

---

# ISSUES INICIAIS (criar em sequencia no GH)

Copie e cole no terminal depois do bootstrap:

```bash
# Wave 1
gh issue create --title "W1.1: Interface IController + backend pydualsense" \
  --label "P1-high,type:feature,ai-task,status:ready" \
  --body "Ver HEFESTO_PROJECT.md secao Wave 1 Sprint 1.1"

gh issue create --title "W1.2: Event bus + state store thread-safe" \
  --label "P1-high,type:feature,ai-task,status:ready" \
  --body "Ver HEFESTO_PROJECT.md secao Wave 1 Sprint 1.2"

gh issue create --title "W1.3: Daemon loop asyncio basico" \
  --label "P1-high,type:feature,ai-task,status:ready" \
  --body "Ver HEFESTO_PROJECT.md secao Wave 1 Sprint 1.3"

# Wave 2
gh issue create --title "W2.1: Trigger effects - 19 modos com factories" \
  --label "P1-high,type:feature,ai-task,status:ready" \
  --body "Ver HEFESTO_PROJECT.md secao Wave 2 Sprint 2.1"

gh issue create --title "W2.2: LED control + rumble com throttle" \
  --label "P2-medium,type:feature,ai-task,status:ready" \
  --body "Ver HEFESTO_PROJECT.md secao Wave 2 Sprint 2.2"

# ... (repetir pras waves 3-8)
```

---

# ADRs CRITICAS PRA ESCREVER ANTES DE CODAR

## ADR-001: Escolha de pydualsense como backend HID

**Decisao:** Usar `pydualsense` (flok, MIT) como backend ao inves de implementar o protocolo HID do zero ou portar C++ do `trigger-control`.

**Contexto:**
- pydualsense tem 115 commits, mantido, MIT, Python puro com hidapi.
- Implementar HID de raiz adicionaria 2 sprints (W1.1 + W1.2 inteiras) pra reinventar algo testado.
- trigger-control e C++: adicionaria complexidade de FFI.

**Consequencias:**
- (+) Velocidade de desenvolvimento.
- (+) Upstream pra contribuir bugfixes.
- (-) Dependencia externa: se flok arquivar, forkamos (MIT permite).
- (-) Performance: Python loop a 60Hz de HID poll e OK; 1000Hz talvez nao.

**Mitigacao:** Interface `IController` abstrai backend. Se precisar, troca-se por impl em C via cffi sem mexer no resto.

## ADR-003: UDP na porta 6969 pra compat com DSX

**Decisao:** Escutar UDP em `127.0.0.1:6969`, mesmo schema JSON do DSX.

**Contexto:** Mods pra Cyberpunk, Forza, Assetto Corsa ja existem e escrevem nessa porta. Rodando o jogo via Proton, o mod roda dentro da maquina virtual Win mas a rede e host-shared. Se Hefesto escuta na mesma porta, mods funcionam sem modificacao.

**Validacao:** Testar com `DualSenseAT` e `DualSenseXLink` via Proton.

## ADR-007: Deferir Wayland pra v1.x

**Decisao:** v0.x so suporta X11 pra deteccao de janela ativa. Wayland retorna "unknown window" e cai no perfil fallback.

**Motivo:** Wayland nao expoe janela ativa sem portal especifico (varia entre GNOME/KDE/Sway). GNOME tem D-Bus extension, KDE tem KWin script, Sway tem IPC proprio. Implementar os 3 fura escopo.

**Workaround v0.x:** CLI + hotkey manual + autoswitch por processo via `/proc/*/comm` quando disponivel.

---

# PADROES DE CODIGO

```python
# PROIBIDO
except: pass                          # nunca engolir
time.sleep()                          # em corotina, use await asyncio.sleep
"/home/user/..."                      # use XDG paths
# by Claude / # generated              # NUNCA

# OBRIGATORIO
from hefesto.utils.logging_config import get_logger
logger = get_logger(__name__)

try:
    ...
except SpecificError as e:
    logger.error("falha ao fazer X", extra={"err": str(e)})
    raise

# paths
from hefesto.utils.xdg_paths import config_dir
path = config_dir() / "profiles" / f"{name}.json"

# async
async def fetch_battery(controller: IController) -> int:
    return await controller.get_battery()
```

---

# LABELS GITHUB

| Label                | Uso                                       |
|----------------------|-------------------------------------------|
| `P0-critical`        | Bug que impede uso                        |
| `P1-high`            | Proxima sprint                            |
| `P2-medium`          | Backlog curto                             |
| `P3-low`             | Nice to have                              |
| `type:feature`       | Novo comportamento                        |
| `type:refactor`      | Codigo muda, comportamento nao            |
| `type:docs`          | Documentacao                              |
| `type:infra`         | CI, packaging, scripts                    |
| `type:test`          | Testes novos                              |
| `status:ready`       | Disponivel pra pegar                      |
| `status:in-progress` | Em execucao                               |
| `status:blocked`     | Bloqueada (comentar motivo)               |
| `ai-task`            | Executavel por IA autonoma                |
| `needs-device`       | Precisa DualSense fisico pra testar       |

---

# CHECKLIST PRE-MERGE

```bash
# 1. Anonimato
./scripts/check_anonymity.sh

# 2. Testes de dados
./scripts/check_test_data.sh

# 3. Lint + types
ruff check src/ tests/
mypy src/hefesto

# 4. Testes
pytest tests/unit -v
pytest tests/integration -v  # se houver device

# 5. Import sanity
python -c "from hefesto.core.controller import IController; print('OK')"

# 6. Commit impessoal
git log -1 --format="%s"  # ver se e "feat: X" e nao "by ..."
```

---

# FLUXO COMPLETO POR ISSUE

```
gh issue list --label "status:ready" --label "ai-task"
         ↓
gh issue edit N --add-label "status:in-progress" --remove-label "status:ready"
         ↓
gh issue develop N --checkout
         ↓
      [implementar + testar]
         ↓
./scripts/check_anonymity.sh    → VAZIO
ruff check && mypy && pytest    → TUDO VERDE
         ↓
git commit -m "feat: W1.1 implementar IController + backend pydualsense"
         ↓
gh pr create --body "Closes #N"
         ↓
gh pr merge --squash --delete-branch
```

---

# DADOS DE TESTE — PROIBIDO

- Nomes reais, paths `/home/<usuario>`, emails pessoais.

# DADOS DE TESTE — PERMITIDO

- `test_user`, `player_1`, `TestProfile`
- `tempfile.mkdtemp()`, `/tmp/hefesto_test_*`
- `test@example.com`, `00:00:00:00:00:00` (MAC)
- VID/PID reais sao OK (protocolo publico).

---

# NAO FACA

- (NAO) Chamar `subprocess.run("xdotool ...")` — use `python-xlib`.
- (NAO) `time.sleep()` em corrotina.
- (NAO) `print()` — use `logger`.
- (NAO) Hardcode de paths — use `xdg_paths`.
- (NAO) Dependencias Windows (pyvgamepad, win32api).
- (NAO) UI como sudo. Daemon como user. Udev rule uma vez.
- (NAO) Escrever "by Claude" em lugar nenhum.
- (NAO) Commit com nome proprio: `git config user.name` pode ser impessoal no repo (`hefesto-dev`).

---

# REFERENCIAS

- DSX Windows original: https://github.com/Paliverse/DualSenseX
- pydualsense (backend): https://github.com/flok/pydualsense
- trigger-control (ref C++): https://github.com/Etaash-mathamsetty/trigger-control
- dualsense-controller (alternativa): https://github.com/yesbotics/dualsense-controller-python
- Protocol HID DualSense: https://controllers.fandom.com/wiki/Sony_DualSense

---

*"A forja nao revela o ferreiro. So a espada."*
